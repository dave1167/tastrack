<!-- Wappler include head-page="layouts/main_shell" fontawesome_5="cdn" bootstrap5="local" is="dmx-app" id="delete_team_role" appConnect="local" components="{dmxBootstrap5TableGenerator:{},dmxStateManagement:{}}" -->
<dmx-serverconnect id="scTeamRole" url="/api/teams/get_team_role" dmx-param:id="query.id"></dmx-serverconnect>

<div class="row g-4">
    <div class="col-12">
        <div class="d-flex align-items-center justify-content-between gap-3">
            <div>
                <h1 class="mb-1">Delete Team Role</h1>
                <p class="text-muted mb-0">Review the team role before deleting it.</p>
            </div>
            <a class="btn btn-outline-secondary" href="/teams/team_roles">Back to team roles</a>
        </div>
    </div>

    <div class="col-12 col-xl-6">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white">
                <h2 class="h5 mb-0">Confirm deletion</h2>
            </div>
            <div class="card-body">
                <div class="alert alert-warning mb-4" role="alert">
                    This will permanently delete this team role.
                </div>

                <dl class="row mb-4">
                    <dt class="col-sm-4">Team</dt>
                    <dd class="col-sm-8" dmx-text="scTeamRole.data.query[0].teamName"></dd>

                    <dt class="col-sm-4">Role</dt>
                    <dd class="col-sm-8" dmx-text="scTeamRole.data.query[0].teamRoleName"></dd>
                </dl>

                <form method="post" dmx-bind:action="'/api/teams/delete_team_role?id=' + query.id">
                    <%- _csrfInput() %>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-danger">Delete Team Role</button>
                        <a class="btn btn-light" href="/teams/team_roles">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<meta name="ac:route" content="/teams/delete_team_role">
